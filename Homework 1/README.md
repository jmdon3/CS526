Coding Assignment HW1
I wrote the code for this in Jupyter Notebook. The code is saved as jdonahue-hw1.py. First I imported 2 libraries. One that allowed for the end user to put a file with the code as a standard in that would then return the text of the standard in file into the command window. The other that would enable the code to test whether or not the file type in standard in would be compatible with the key part of the code. Then I have an if statement testing the standard in file type that returns a message depending on the result and either proceeds or exits the code. Finally I used a for loop that takes the file from the standard in and returns the text of that file into the command window as specified. To test this code, I downloaded it as an executable script (.py) and ran it in Git Bash with my test file (myfile.txt) as a standard in.

Introduce Yourself
1. One programming class. Intro to Python last fall at BU
2. Python
3. 2nd year of my masters program
4. I learned basics of object oriented programming in Python in my intro class
